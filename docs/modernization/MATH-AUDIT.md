# Mathematical and algorithmic audit

A formula can be algebraically valid and still optimize the wrong quantity. Separate arithmetic, statistical assumptions, proxy validity, operational bounds and measured user outcomes. Numeric results below are source-derived arithmetic, not independent replications of historical model experiments.

| System | Implementation / intended idea | Verdict and better implementation | Required validation |
|---|---|---|---|
| Finite brain | estimated bytes/4 ASCII, /2 other text, fixed image estimate; fractional allocation | A useful heuristic, not a capacity guarantee. P04 final wire-token invariant with estimator uncertainty; count tool schemas/volatile additions. | actual token fixtures across languages, images, huge fixed prompt; never send oversize |
| λ decay | exponential decay with lambda .01/hour | half-life ln(2)/lambda =69.3147h=2.888d, not29d. Preserve tiered recall, expose units and configure half-life directly. | boundary times, future timestamps, repeated recall and GC |
| Artifact value | Q×R×U priority | A ranking heuristic, not proof that a greedy token pack is globally most valuable. Define utility target and compare value-density/knapsack approximations on replay. Multiplicative floors can hide invalid negative values. | finite values, monotonicity, recall success at equal budget, task/user strata |
| Engram | bounded importance updates, exponential time constant tau, hysteresis and pinning | EMA/decay formulas are coherent, but EMA is not wired to production fact updates (curator assigns4.0; tool replaces). Checkpoint42 repairs nonfinite inputs/thresholds and packing overflow; see ENGRAM-NUMERIC-IMPLEMENTATION.md. tau60d means half-life41.59d. Confidence is not factual truth. Pin explicit user facts; retain provenance and contradiction/supersession links. | stale/contradictory memory, multilingual facts, bounded records, deletion |
| Learning | Beta mean alpha/(alpha+beta), exp(-.015 days), 1+.3 ln(1+uses) | Recency half-life46.21d. Prior from LLM c gives alpha2+3c, beta2+3(1-c), mean range2/7..5/7: shrinkage is reasonable; uses measure exposure, not benefit. | counterfactual held-out tasks; do not reinforce merely because retrieved |
| Blueprint | success/usage-based quality and retrieval hints | Match confidence is not execution applicability. Check preconditions, tool/schema versions, evidence-based outcomes and negative transfer; compare replay vs planning cost. | stale credentials/UI/tool versions, partial completion, destructive recipe |
| Wilson | standard score interval in distill/stats/wilson.rs | Formula matches score interval for valid binomial counts. 29/30 @99% lower=.768206; 30/30=.818872. Need at least127 perfect trials for >=.95 using z2.576. Lookup clamps confidence outside .80–.99 and interpolates z: replace with validated inverse CDF. | k<=n; finite confidence; n0 explicitly unknown; independent ground-truth labels |
| Power | stats/power.rs uses same z lookup for alpha and power | One-sided power .80 requires Phi^-1(.80)≈.84162, not1.282. For its equal-variance approximation with p0=.5,p1=.55, alpha.05, n≈785 rather than1052. Specify one-/two-sample design; use p0 and p1 variances or exact binomial power as appropriate. | reference quantiles, Monte Carlo actual type-I/power, invalid domain |
| SPRT | sum log Bernoulli likelihood ratios; Wald boundaries | Updates are coherent for iid Bernoulli under fixed hypotheses. Truncation sign rule can graduate below acceptance threshold; nominal errors no longer guaranteed. Return Inconclusive at cap or calibrate a finite-horizon rule. | exact DP/simulation over p0,p1, correlation, truncation, repeated resets |
| CUSUM | max(0,S+target-x-slack), threshold alarm, optional half-threshold start | Coherent lower-quality shift detector. Threshold is not itself a confidence percentage; correlated proxy observations change false alarms. | in-control average run length and detection delay by real workload; preserve state |
| Beta/Thompson | conjugate updates, Gamma-based beta draws, category selection | Valid sampling algorithm for positive parameters, but rewarding high-quality categories does not enforce balanced diversity. Separate quality filtering from explicit stratified quotas. | invalid parameters, reproducible seeds, finite samples, category starvation |
| Shannon diversity | H/ln(K observed nonzero categories) | Measures observed evenness; two equal categories score1 even if eight needed categories are absent. Define fixed taxonomy coverage separately; no universal diversity claim. | missing categories, unknown category, skew, entropy after selection |
| Eigen quality | embedding agreement and continue/retry behavior | Neither is ground-truth correctness or absence of hallucination. Similar wrong answers agree; silence may mean abandonment. Use task-specific executable labels plus blind human subset; group holdout by task/session/time to avoid near-duplicate leakage. | train/eval contamination, domain-specific confidence, shadow-only opt-in |
| Hive | A^alpha U^beta(1-D)^gamma(1-F)^delta R^zeta | Bounded/floored scores are heuristic priority, not an optimal scheduler. Three pheromone queries per ready task create scaling overhead. Batch reads; benchmark critical path, aging/fairness and capability feasibility while preserving decentralized coordination. | blocked prerequisites, stale lease, failure recovery, starvation, cost vs sequential |
| Perpetuum activity | positive rows / rows from active-only table | Always1 for observed ordinary data; not an exposure estimate. Use active calendar slots / observed eligible slots, scoped by user/timezone, unknown when offline. Beta prior optional; document denominator. | one active /28 observed; missing data; DST; actual four-week cutoff |
| Perpetuum schedule | cron conversion + chrono timezone | Keep real timezone arithmetic; specify DST ambiguous/missing times and catch-up/coalescing after outage. Duration and calendar recurrence are different. | DST fold/gap, clock jump, overdue single alarm, restart claim lease |
| Consciousness | LLM critique and budget checks | Extra opinion is not independent truth. Its reported cost/success deltas from small experiments are hypotheses; verify downstream evidence and include ALL calls. | paired randomization, per-task costs, confidence intervals, ablation by mode |
| Anima | LLM trait/confidence + raw text features, thresholds | Thresholds express policy, not calibrated probability. Byte/character mismatch biases language comparison. Keep work/word separation, correct units, use abstention and calibration. | creator-defined ethics invariants plus multilingual fixtures |
| Cambium trust | streak thresholds10/25, rollback count3 | No statistical guarantee; general task verdicts do not prove safe code growth. Scope trust to capability/zone and verifiable growth outcomes; do not auto-expand capability from prose. | broken integration with compiling code; rollback; protected-zone changes |
| Gaze | pixel delta thresholds, region difference | O(pixel count) observation method is reasonable. Screen change not task completion; no-change not safe-to-repeat. Separate intent/evidence. | invisible side effects, spinner, scale and monitor origins |
| Search fusion | weighted provider scores and dedup | Provider scores may not share calibration. Benchmark reciprocal rank fusion or normalized scores rather than assuming weighted numbers comparable. Preserve source attribution and diversity. | relevance judged across domains, duplicate URL normalization, freshness/sort |
| Retry | capped exponential delay + deterministic jitter | Positive delays do not prevent synchronized clients. Randomize, honor server not-before, coordinate by account and avoid retrying uncertain writes. | burst load, >60s Retry-After, cancellation, non-idempotent timeout |
| Budget/cache | summed token costs; cache fields omitted | Atomic accounting after calls is not reservation before concurrent calls. Normalize category rates, reserve bounded worst-case usage, reconcile actuals and unknown usage. Subscription quota is not token-dollar spend. | concurrent admission, failed stream with usage, cache write/read, restart |
| Snapshot/patch | git trees and sequential renames | Atomic rename of one file is not atomic multi-file commit. Separate index and journal mutations; preserve unrelated files. | staged/unstaged states, create/delete/rename, disk failure, kill/restart |

No arbitrary heuristic should be replaced merely because it is a heuristic. Keep cheap mechanical filters when they are measured and bounded; use the LLM for semantic decisions while requiring evidence for claimed results. The creator's infrastructure/intelligence distinction does not require making token counting, scheduling clocks or authorization probabilistic.

## Implementation update: normal quantiles and invalid statistical inputs

Wilson intervals now use a validated inverse standard-normal CDF rather than interpolating/clamping a five-row table. Configured confidence above 99% is honored. The checked API rejects zero trials, impossible counts and invalid confidence; its compatibility wrapper returns uninformative `[0,1]`, and graduation paths use checked values. Evaluation accuracy/threshold validation prevents malformed records from producing graduation evidence. Reference: [NIST Wilson interval](https://www.itl.nist.gov/div898/handbook/prc/section2/prc241.htm).

Using the unrounded 99% critical value gives lower bounds **0.768224** for 29/30 and **0.818891** for 30/30; the audit table above records the earlier rounded z=2.576 calculation. Neither supports a 95% lower bound. Independent Python NormalDist fixtures cover confidence from 10% through 99.9999999999%. The same inverse CDF replaces the power estimator's coarse rational approximation; lower-tail evaluation avoids subtraction rounding at tiny alpha, and underflow/nonfinite estimates return no finite sample size rather than zero.

The scalar distribution implementation uses `statrs` 0.18 with default features disabled. Its declared Rust minimum is 1.65; 0.19.1 requires 1.89 and is not introduced solely to obtain the same scalar API. Wilson remains an approximate binomial interval with independent-trial assumptions. Correct quantiles do not establish that cheap answer-equivalence labels are ground truth or that repeated adaptive testing has fixed-sample coverage.
